version: "3.8"

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        environment:
            - PNPM_HOME=/pnpm
            - PATH=/pnpm:$PATH
        volumes:
            - .:/app
        command: ["pnpm", "start"]
